import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nugroho Try Handoko — Renewable Energy Engineer",
  description:
    "Portfolio of Nugroho Try Handoko, a Renewable Energy Engineer specializing in electrical systems, SOLAR PV, biogas, and micro-hydro power.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#0a0d12] text-[#e6e9ef]">
        {children}
      </body>
    </html>
  );
}